'use client'

import React, { useState, useRef } from 'react'
import { useRouter } from 'next/navigation'
import { Search, Plus, ChevronDown, MoreVertical, X, Calendar, ArrowRight } from 'lucide-react'
import DatePicker from 'react-datepicker'
import 'react-datepicker/dist/react-datepicker.css'

interface UserData {
  id: string
  firstName: string
  lastName: string
  email: string
  status: 'Active' | 'Inactive'
  role: string
  startDate: string
  endDate?: string
  trial: string
}

export default function DashboardPage() {
  const router = useRouter()
  
  const sampleUsers: UserData[] = [
    { id: '1', firstName: 'Audra', lastName: 'Luettgen', email: 'Audra.Luettgen@domain.ext', status: 'Active', role: 'Study Coord.', startDate: '15/01/2024', endDate: '15/12/2024', trial: 'GSXK Diabetes' },
    { id: '2', firstName: 'Augustus', lastName: 'Daugherty', email: 'Augustus.Daugherty@domain.ext', status: 'Active', role: 'Site Coord.', startDate: '20/02/2024', endDate: '20/11/2024', trial: 'GSXK Diabetes' },
    { id: '3', firstName: 'Dessie', lastName: 'Grady', email: 'Dessie.Grady@domain.ext', status: 'Active', role: 'Site Coord.', startDate: '10/03/2024', endDate: '10/10/2024', trial: 'GSXK Diabetes' },
    { id: '4', firstName: 'Selina', lastName: 'Abernathy', email: 'Selina.Abernathy@domain.ext', status: 'Active', role: 'Site Coord.', startDate: '05/04/2024', endDate: '05/09/2024', trial: 'GKR4 Diabetes' },
    { id: '5', firstName: 'Lyda', lastName: 'Gulgowski', email: 'Lyda.Gulgowski@domain.ext', status: 'Active', role: 'Site Coord.', startDate: '12/05/2024', endDate: '12/08/2024', trial: 'GKR4 Diabetes' },
    { id: '6', firstName: 'Lola', lastName: 'Rutherford', email: 'Lola.Rutherford@domain.ext', status: 'Inactive', role: 'Site Coord.', startDate: '01/06/2024', endDate: '01/07/2024', trial: 'GKR4 Diabetes' }
  ]

  const [search, setSearch] = useState('')
  const [trialFilter, setTrialFilter] = useState('')
  const [statusFilter, setStatusFilter] = useState('')
  const [fromDate, setFromDate] = useState<Date | null>(null)
  const [toDate, setToDate] = useState<Date | null>(null)
  const [tempFromDate, setTempFromDate] = useState<Date | null>(null)
  const [tempToDate, setTempToDate] = useState<Date | null>(null)
  const [expandedTrials, setExpandedTrials] = useState<Record<string, boolean>>({ 'GSXK Diabetes': true })
  const [showDropdown, setShowDropdown] = useState<string | null>(null)
  const [showDeleteModal, setShowDeleteModal] = useState<boolean>(false)
  const [userToDelete, setUserToDelete] = useState<UserData | null>(null)
  const [showRemoveModal, setShowRemoveModal] = useState<boolean>(false)
  const [userToRemove, setUserToRemove] = useState<UserData | null>(null)
  const [showRemoveSuccessModal, setShowRemoveSuccessModal] = useState<boolean>(false)
  const [removedUser, setRemovedUser] = useState<UserData | null>(null)
  const [users, setUsers] = useState<UserData[]>(sampleUsers)
  const fromDateRef = useRef<any>(null)
  const toDateRef = useRef<any>(null)

  const trials = [
    { name: 'GSXK Diabetes', count: 50 },
    { name: 'GKR4 Diabetes', count: 20 }
  ]

  const filteredUsers = users.filter(user => {
    if (search && !user.firstName.toLowerCase().includes(search.toLowerCase()) &&
        !user.lastName.toLowerCase().includes(search.toLowerCase()) &&
        !user.email.toLowerCase().includes(search.toLowerCase()) &&
        !user.status.toLowerCase().includes(search.toLowerCase()) &&
        !user.role.toLowerCase().includes(search.toLowerCase())) {
      return false
    }

    if (trialFilter && user.trial !== trialFilter) {
      return false
    }

    if (statusFilter && user.status.toLowerCase() !== statusFilter.toLowerCase()) {
      return false
    }

    if (fromDate || toDate) {
      const userStartDate = parseDate(user.startDate)
      if (!userStartDate) return false
      if (fromDate && userStartDate < fromDate) return false
      if (toDate && userStartDate > toDate) return false
    }

    return true
  })

  function parseDate(dateStr: string): Date | null {
    if (!dateStr) return null
    const [day, month, year] = dateStr.split('/').map(Number)
    return new Date(year, month - 1, day)
  }

  const toggleTrial = (trialName: string) => {
    setExpandedTrials(prev => ({
      ...prev,
      [trialName]: !prev[trialName]
    }))
  }

  const getStatusDot = (status: string) => (
    <div className={`w-2 h-2 rounded-full ${status === 'Active' ? 'bg-green-500' : 'bg-red-500'}`}></div>
  )

  const handleUserAction = (user: UserData, event: React.MouseEvent) => {
    event.stopPropagation()
    setShowDropdown(showDropdown === user.id ? null : user.id)
  }

  const handleAddUser = () => {
    console.log('Add User button clicked, navigating to /add-user')
    router.push('/add-user')
  }

  const closeDropdown = () => setShowDropdown(null)

  const handleDeleteUser = (user: UserData) => {
    setUserToDelete(user)
    setShowDeleteModal(true)
    setShowDropdown(null)
  }

  const confirmDeleteUser = () => {
    if (userToDelete) {
      setUsers(users.filter(user => user.id !== userToDelete.id))
      setShowDeleteModal(false)
      setUserToDelete(null)
    }
  }

  const cancelDeleteUser = () => {
    setShowDeleteModal(false)
    setUserToDelete(null)
  }

  const handleRemoveFromTrial = (user: UserData) => {
    setUserToRemove(user)
    setShowRemoveModal(true)
    setShowDropdown(null)
  }

  const confirmRemoveFromTrial = () => {
    if (userToRemove) {
      // Update user status to Inactive
      const updatedUsers = users.map(user => 
        user.id === userToRemove.id 
          ? { ...user, status: 'Inactive' as 'Inactive' }
          : user
      )
      setUsers(updatedUsers)
      
      // Store the removed user data with updated status for success modal
      const updatedUser = { ...userToRemove, status: 'Inactive' as 'Inactive' }
      setRemovedUser(updatedUser)
      
      // Close the confirmation modal
      setShowRemoveModal(false)
      setUserToRemove(null)
      
      // Show success modal
      setShowRemoveSuccessModal(true)
    }
  }

  const cancelRemoveFromTrial = () => {
    setShowRemoveModal(false)
    setUserToRemove(null)
  }

  const closeRemoveSuccessModal = () => {
    setShowRemoveSuccessModal(false)
    setRemovedUser(null)
  }

  const applyDateFilter = () => {
    setFromDate(tempFromDate)
    setToDate(tempToDate)
  }

  const renderColGroup = () => (
    <colgroup>
      <col style={{ width: '15%' }} />
      <col style={{ width: '15%' }} />
      <col style={{ width: '25%' }} />
      <col style={{ width: '10%' }} />
      <col style={{ width: '12%' }} />
      <col style={{ width: '10%' }} />
      <col style={{ width: '10%' }} />
      <col style={{ width: '3%' }} />
    </colgroup>
  )

  return (
    <div className="min-h-screen bg-white mt-4 px-3 sm:px-6 md:px-10 py-4">
      <div className="mb-4 border rounded-2xl overflow-hidden shadow-sm">
        <div className="max-w-[1300px]">
          {/* Header table (no scroll) */}
          <div className="overflow-x-auto">
            <table className="w-full min-w-[900px] text-base border-collapse table-auto sm:table-fixed">
              {renderColGroup()}
              <thead className="bg-gray-50 border-b border-gray-300 sticky top-0 z-10">
                {/* Controls Row */}
                <tr>
                  <th colSpan={8} className="px-6 py-4 border-b border-gray-200 bg-gray-100">
                    <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-2 sm:gap-4">
                      {/* Title - Always Left */}
                      <div className="text-left w-full sm:w-auto">
                        <h2 className="text-xl font-normal text-gray-800">Users</h2>
                      </div>

                      {/* Controls - Right on Desktop, Stacked on Mobile */}
                      <div className="flex flex-col sm:flex-row items-start sm:items-center gap-2 sm:gap-4">
                        <div className="relative w-[250px] sm:w-64">
                          <input
                            type="search"
                            placeholder="Search"
                            value={search}
                            onChange={(e) => setSearch(e.target.value)}
                            className="w-full sm:w-64 px-4 py-2 font-normal text-sm border rounded-full"
                          />
                          <Search className="absolute right-3 top-2.5 h-4 w-4 text-gray-400" />
                        </div>

                        <button
                          onClick={handleAddUser}
                          className="bg-[#007fb2] text-white px-4 py-2 h-9 rounded-full text-sm flex items-center gap-2"
                        >
                          <Plus className="h-4 w-4" />
                          <span>Add</span>
                        </button>
                      </div>
                    </div>
                  </th>
                </tr>

                {/* Filters Row */}
                <tr>
                  <th colSpan={8} className="px-6 py-3 border-b border-gray-300">
                    <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 text-xs">
                      {/* FILTERS stacked on left on mobile */}
                      <div className="flex flex-col sm:flex-row sm:items-center gap-2 flex-wrap">
                        <span className="font-semibold">FILTER</span>
                        
                        <select 
                          value={trialFilter} 
                          onChange={(e) => setTrialFilter(e.target.value)} 
                          className="px-2 py-1 w-40 h-9 border bg-gray-100 font-normal border-gray-300 rounded-md text-xs"
                        >
                          <option value="">Trial Name</option>
                          <option value="GSXK Diabetes">GSXK Diabetes</option>
                          <option value="GKR4 Diabetes">GKR4 Diabetes</option>
                        </select>
                        
                        <select 
                          value={statusFilter} 
                          onChange={(e) => setStatusFilter(e.target.value)} 
                          className="px-2 py-1 w-28 h-9 bg-gray-100 font-normal border-gray-300 rounded-md border text-xs"
                        >
                          <option value="">Status</option>
                          <option value="active">Active</option>
                          <option value="inactive">Inactive</option>
                        </select>

                          {/* DATE Pickers */}
                          <div className="flex items-center gap-2">
                            <span className="font-medium text-gray-700 text-sm">DATE</span>
                            <div className="flex items-center bg-white border border-gray-300 rounded-lg h-9">
                              {/* From Date */}
                              <div className="relative flex items-center px-2 hover:bg-gray-50 rounded-l-lg">
                                <DatePicker
                                  ref={fromDateRef}
                                  selected={tempFromDate}
                                  onChange={(date) => setTempFromDate(date)}
                                  placeholderText="From"
                                  className="w-12 text-xs text-gray-500 placeholder-gray-500 bg-transparent cursor-pointer"
                                  dateFormat="dd/MM/yyyy"
                                  wrapperClassName="date-picker-wrapper"
                                  popperClassName="date-picker-high-z-index"
                                  popperPlacement="bottom-start"
                                />
                                <Calendar 
                                  className="h-3 w-3 text-gray-500 ml-1 h-full cursor-pointer" 
                                  onClick={() => fromDateRef.current?.setFocus()}
                                />
                              </div>
                              
                              {/* Divider */}
                              <div className="h-5 w-px bg-gray-300"></div>
                              
                              {/* To Date */}
                              <div className="relative flex items-center px-2 hover:bg-gray-50">
                                <DatePicker
                                  ref={toDateRef}
                                  selected={tempToDate}
                                  onChange={(date) => setTempToDate(date)}
                                  placeholderText="To"
                                  className="w-12 text-xs text-gray-500 placeholder-gray-500 bg-transparent cursor-pointer"
                                  dateFormat="dd/MM/yyyy"
                                  wrapperClassName="date-picker-wrapper"
                                  popperClassName="date-picker-high-z-index"
                                  popperPlacement="bottom-start"
                                />
                                <Calendar 
                                  className="h-3 w-3 text-gray-500 ml-1 cursor-pointer" 
                                  onClick={() => toDateRef.current?.setFocus()}
                                />
                              </div>
                              
                              {/* Apply Button - Integrated */}
                              <button 
                                className="bg-gray-500 hover:bg-gray-600 text-white px-2 h-full transition-colors flex items-center justify-center rounded-r-lg"
                                onClick={applyDateFilter}
                              >
                                <ArrowRight className="h-3 w-3" />
                              </button>
                            </div>
                            
                          {/* Clear button integrated with date filter */}
                          {(trialFilter || statusFilter || fromDate || toDate) && (
                            <button 
                              onClick={() => { 
                                setTrialFilter(''); 
                                setStatusFilter(''); 
                                setFromDate(null); 
                                setToDate(null); 
                                setTempFromDate(null); 
                                setTempToDate(null);
                              }} 
                              className="text-gray-400 hover:text-gray-600"
                            >
                              <X className="h-3 w-3" />
                            </button>
                          )}
                        </div>
                      </div>
                    </div>
                  </th>
                </tr>
              </thead>
            </table>
          </div>

          {/* Scrollable body table */}
          <div className="max-h-[150px] md:max-h-[400px] overflow-y-auto overflow-x-auto">
            <table className="w-full min-w-[900px] text-base border-collapse table-auto sm:table-fixed">
              {renderColGroup()}
              <tbody>
                {trials.map(trial => {
                  const trialUsers = filteredUsers.filter(user => user.trial === trial.name)
                  return (
                    <React.Fragment key={trial.name}>
                      <tr className="bg-gray-100">
                        <td colSpan={8} className="px-4 py-1 border-b">
                          <button
                            onClick={() => toggleTrial(trial.name)}
                            className="w-full flex rounded-2xl justify-between items-center bg-gray-100 px-4 py-3 text-left font-medium text-gray-700 hover:bg-gray-100"
                          >
                            <span className="flex items-center gap-2">
                              <ChevronDown className={`h-4 w-4 transition-transform ${expandedTrials[trial.name] ? 'rotate-180' : ''}`} />
                              {trial.name} ({trialUsers.length})
                            </span>
                          </button>
                        </td>
                      </tr>

                      {expandedTrials[trial.name] && (
                        <>
                          {/* Table headers per group */}
                          <tr className="bg-gray-50 text-gray-700 font-semibold text-xs">
                            <td className="px-3 py-2">First name</td>
                            <td className="px-3 py-2">Last name</td>
                            <td className="px-3 py-2">Email</td>
                            <td className="px-3 py-2">Status</td>
                            <td className="px-3 py-2">Role</td>
                            <td className="px-3 py-2">Start date</td>
                            <td className="px-3 py-2">End date</td>
                            <td className="px-3 py-2"></td>
                          </tr>

                          {/* Table rows */}
                          {trialUsers.length === 0 ? (
                            <tr className="bg-white">
                              <td colSpan={8} className="text-center py-20 text-gray-500 text-sm">
                                No users found.
                              </td>
                            </tr>
                          ) : (
                            trialUsers.map(user => (
                              <tr key={user.id} className="border-b bg-white hover:bg-gray-50 text-xs">
                                <td className="px-3 py-2">{user.firstName}</td>
                                <td className="px-3 py-2">{user.lastName}</td>
                                <td className="px-3 py-2">{user.email}</td>
                                <td className="px-3 py-2">
                                  <div className="flex items-center space-x-1">
                                    {getStatusDot(user.status)}
                                    <span>{user.status}</span>
                                  </div>
                                </td>
                                <td className="px-3 py-2">{user.role}</td>
                                <td className="px-3 py-2">{user.startDate}</td>
                                <td className="px-3 py-2">{user.endDate || '-'}</td>
                                <td className="px-3 py-2 relative">
                                  <button 
                                    onClick={(e) => handleUserAction(user, e)} 
                                    className="p-1 hover:bg-gray-100 rounded"
                                  >
                                    <MoreVertical className="h-4 w-4 text-gray-400" />
                                  </button>
                                  {showDropdown === user.id && (
                                    <div className="absolute right-0 mt-1 w-32 bg-white rounded-md shadow-lg z-10 border text-xs">
                                      <a href="/edit-user" className="block px-3 py-2 text-gray-700 hover:bg-gray-100">Edit</a>
                                      <button 
                                        onClick={() => handleRemoveFromTrial(user)}
                                        className="block w-full text-left px-3 py-2 text-gray-700 hover:bg-gray-100"
                                      >
                                        Remove from trial
                                      </button>
                                      <button 
                                        onClick={() => handleDeleteUser(user)}
                                        className="block w-full text-left px-3 py-2 text-red-600 hover:bg-gray-100"
                                      >
                                        Delete
                                      </button>
                                    </div>
                                  )}
                                </td>
                              </tr>
                            ))
                          )}
                        </>
                      )}
                    </React.Fragment>
                  )
                })}
              </tbody>
            </table>
          </div>
        </div>
      </div>

      {/* Delete Confirmation Modal */}
      {showDeleteModal && userToDelete && (
        <div className="fixed inset-0 bg-gray-900 bg-opacity-50 flex items-center justify-center z-50">
          <div className="bg-white rounded-2xl shadow-xl w-full max-w-md mx-4">
            {/* Modal Header */}
            <div className="flex items-center justify-between p-4 border-b border-gray-200">
              <h2 className="text-lg font-semibold text-gray-900">Delete User</h2>
              <button
                onClick={cancelDeleteUser}
                className="p-2 hover:bg-gray-100 rounded-full transition-colors"
              >
                <X className="h-5 w-5 text-gray-600" />
              </button>
            </div>

            {/* Modal Content */}
            <div className="p-6">
              {/* Warning Section */}
              <div className="flex items-start space-x-4 mb-6">
                {/* Warning Icon */}
                <div className="w-12 h-12 bg-red-100 rounded-full flex items-center justify-center flex-shrink-0">
                  <span className="text-red-600 text-xl font-bold">!</span>
                </div>
                
                {/* Warning Text */}
                <div className="flex-1">
                  <h3 className="text-md font-semibold text-gray-900 mb-1">
                    Are you sure you want to delete this user?
                  </h3>
                  <p className="text-md font-semibold text-gray-900">
                    This action can not be undone.
                  </p>
                </div>
              </div>

              {/* Divider */}
              <div className="border-t border-gray-200 mb-6"></div>

              {/* User Details */}
              <div className="grid grid-cols-2 gap-4 mb-6">
                {/* Left Column - User Info */}
                <div>
                  <div className="font-semibold text-gray-900 text-sm mb-1">
                    {userToDelete.firstName} {userToDelete.lastName}
                  </div>
                  <div className="text-xs text-gray-600">
                    {userToDelete.email}
                  </div>
                </div>

                {/* Right Column - Role and Trials */}
                <div>
                  <div className="font-semibold text-gray-900 text-sm mb-1">
                    {userToDelete.role}
                  </div>
                  <div className="text-xs text-gray-600 space-y-1">
                    <div>Repudiandae assumenda</div>
                    <div>Tenetur non et</div>
                    <div>Eos qui reiciendis</div>
                  </div>
                </div>
              </div>

              {/* Action Buttons */}
              <div className="flex justify-end space-x-3">
                <button
                  onClick={cancelDeleteUser}
                  className="flex items-center space-x-2 px-4 py-2 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 transition-colors"
                >
                  <span>Cancel</span>
                  <X className="h-4 w-4" />
                </button>
                <button
                  onClick={confirmDeleteUser}
                  className="flex items-center space-x-2 px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700 transition-colors"
                >
                  <span>Delete User</span>
                  <svg className="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                  </svg>
                </button>
              </div>
            </div>
          </div>
        </div>
      )}

      {/* Remove from Trial Confirmation Modal */}
      {showRemoveModal && userToRemove && (
        <div className="fixed inset-0 bg-gray-900 bg-opacity-50 flex items-center justify-center z-50">
          <div className="bg-white rounded-2xl shadow-xl w-full max-w-md mx-4">
            {/* Modal Header */}
            <div className="flex items-center justify-between p-4 border-b border-gray-200">
              <h2 className="text-lg font-semibold text-gray-900">Remove from Trial</h2>
              <button
                onClick={cancelRemoveFromTrial}
                className="p-2 hover:bg-gray-100 rounded-full transition-colors"
              >
                <X className="h-5 w-5 text-gray-600" />
              </button>
            </div>

            {/* Modal Content */}
            <div className="p-6">
              {/* Info Icon */}
              <div className="flex items-center justify-center mb-4">
                <div className="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center">
                  <svg className="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                </div>
              </div>

              {/* Confirmation Message */}
              <div className="text-center mb-6">
                <h3 className="text-lg font-medium text-gray-900 mb-2">Remove from Clinical Trial?</h3>
                <p className="text-sm text-gray-600">
                  This will remove the user from the current clinical trial but keep them in the system.
                </p>
                <div className="mt-3 p-3 bg-gray-50 rounded-lg">
                  <p className="text-sm font-medium text-gray-900">
                    {userToRemove.firstName} {userToRemove.lastName}
                  </p>
                  <p className="text-xs text-gray-600">{userToRemove.email}</p>
                  <p className="text-xs text-gray-500 mt-1">Current Trial: {userToRemove.trial}</p>
                </div>
              </div>

              {/* Action Buttons */}
              <div className="flex space-x-3">
                <button
                  onClick={cancelRemoveFromTrial}
                  className="flex-1 px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition-colors"
                >
                  Cancel
                </button>
                <button
                  onClick={confirmRemoveFromTrial}
                  className="flex-1 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"
                >
                  Remove from Trial
                </button>
              </div>
            </div>
          </div>
        </div>
      )}

      {/* Remove from Trial Success Modal */}
      {showRemoveSuccessModal && removedUser && (
        <div className="fixed inset-0 bg-gray-900 bg-opacity-50 flex items-center justify-center z-50">
          <div className="bg-white rounded-2xl shadow-xl w-full max-w-md mx-4">
            {/* Modal Header */}
            <div className="flex items-center justify-between p-4 border-b border-gray-200">
              <h2 className="text-lg font-semibold text-gray-900">Remove from Trial</h2>
              <button
                onClick={closeRemoveSuccessModal}
                className="p-2 hover:bg-gray-100 rounded-full transition-colors"
              >
                <X className="h-5 w-5 text-gray-600" />
              </button>
            </div>

            {/* Success Content */}
            <div className="p-6">
              {/* Success Message */}
              <div className="text-center mb-6">
                <div className="flex items-center justify-center mb-3">
                  <div className="w-8 h-8 bg-green-100 rounded-full flex items-center justify-center">
                    <svg className="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 13l4 4L19 7" />
                    </svg>
                  </div>
                </div>
                <h3 className="text-lg font-medium text-gray-900">User removed from trial</h3>
              </div>

              {/* Divider */}
              <div className="border-t border-gray-200 mb-6"></div>

              {/* User Details */}
              <div className="grid grid-cols-2 gap-4 mb-6">
                {/* Left Column - User Info */}
                <div>
                  <div className="font-semibold text-gray-900 text-sm mb-1">
                    {removedUser.firstName} {removedUser.lastName}
                  </div>
                  <div className="text-xs text-gray-600">
                    {removedUser.email}
                  </div>
                </div>

                {/* Right Column - Trial Info */}
                <div>
                  <div className="font-semibold text-gray-900 text-sm mb-1">
                    Removed from
                  </div>
                  <div className="text-xs text-gray-600">
                    {removedUser.trial}
                  </div>
                </div>
              </div>

              {/* Close Button */}
              <div className="flex justify-end">
                <button
                  onClick={closeRemoveSuccessModal}
                  className="flex items-center space-x-2 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"
                >
                  <span>Close</span>
                  <X className="h-4 w-4" />
                </button>
              </div>
            </div>
          </div>
        </div>
      )}
    </div>
  )
}
